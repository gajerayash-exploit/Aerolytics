// Generated by scripts/gen-routes.mjs
import ViewHost from '@/components/ViewHost';
import { pillarOf, sitePillar } from '@/lib/routes';

export const metadata = { title: "Site detail" };

export default async function Page({ params }: { params: Promise<Record<string, string>> }) {
  const p = await params;
  return <ViewHost view="SiteDetail" {...{ id: p.id, pillar: sitePillar(p.id) }} />;
}
